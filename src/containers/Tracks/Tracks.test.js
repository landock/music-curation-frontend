import React from 'react';
import renderer from 'react-test-renderer';
import Tracks from '.';

it('Tracks: default', () => {
  // const component = renderer.create(<Tracks />)
  // const tree = component.toJSON()
  // expect(tree).toMatchSnapshot()
});
