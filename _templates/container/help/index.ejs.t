---
message: |
  hygen {bold container new} --name {bold NAME} 

  Generates a React container using Recycle and a test.

    NAME        The container name in kebab-case ({bold required}).
    --redux     Generate a Redux specific container (optional).
---